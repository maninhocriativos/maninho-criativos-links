import { errorResponse, HttpError, integer, json, readJson, text } from '../_utils.js';
import { requireAuth } from './_auth.js';
const TYPES=new Set(['income','expense']), STATUSES=new Set(['pending','paid','cancelled']);
function parse(body){const type=text(body.type,{required:true,max:10}),status=text(body.status||'pending',{required:true,max:12});if(!TYPES.has(type)||!STATUSES.has(status))throw new HttpError(400,'Tipo ou status inválido');const due=text(body.due_date,{required:true,max:10}),paid=text(body.paid_date,{max:10});if(!/^\d{4}-\d{2}-\d{2}$/.test(due)||(paid&&!/^\d{4}-\d{2}-\d{2}$/.test(paid)))throw new HttpError(400,'Data inválida');return{client_id:body.client_id?integer(body.client_id,{min:1}):null,project_id:body.project_id?integer(body.project_id,{min:1}):null,type,category:text(body.category,{required:true,max:80}),description:text(body.description,{required:true,max:240}),amount_cents:integer(body.amount_cents,{min:1,max:100_000_000}),due_date:due,paid_date:status==='paid'?(paid||due):null,status,payment_method:text(body.payment_method,{max:80}),notes:text(body.notes,{max:500})};}
export async function onRequestGet({request,env}){const denied=await requireAuth(request,env);if(denied)return denied;const url=new URL(request.url),month=url.searchParams.get('month')||new Date().toISOString().slice(0,7);if(!/^\d{4}-\d{2}$/.test(month))return json({error:'Mês inválido'},400);const [rows,summary,monthly,categories]=await Promise.all([env.DB.prepare(`SELECT t.*,c.name client_name,p.title project_title FROM cash_transactions t LEFT JOIN clients c ON c.id=t.client_id LEFT JOIN design_projects p ON p.id=t.project_id WHERE substr(t.due_date,1,7)=? ORDER BY t.due_date DESC,t.id DESC`).bind(month).all(),env.DB.prepare(`SELECT COALESCE(SUM(CASE WHEN type='income' AND status='paid' THEN amount_cents ELSE 0 END),0) income_paid,COALESCE(SUM(CASE WHEN type='expense' AND status='paid' THEN amount_cents ELSE 0 END),0) expense_paid,COALESCE(SUM(CASE WHEN type='income' AND status='pending' THEN amount_cents ELSE 0 END),0) receivable,COALESCE(SUM(CASE WHEN type='expense' AND status='pending' THEN amount_cents ELSE 0 END),0) payable FROM cash_transactions WHERE substr(due_date,1,7)=?`).bind(month).first(),env.DB.prepare(`SELECT substr(due_date,1,7) month,SUM(CASE WHEN type='income' AND status='paid' THEN amount_cents ELSE 0 END) income,SUM(CASE WHEN type='expense' AND status='paid' THEN amount_cents ELSE 0 END) expense FROM cash_transactions WHERE due_date>=date('now','-11 months','start of month') GROUP BY month ORDER BY month`).all(),env.DB.prepare(`SELECT category,type,SUM(amount_cents) total FROM cash_transactions WHERE substr(due_date,1,7)=? AND status='paid' GROUP BY category,type ORDER BY total DESC`).bind(month).all()]);return json({month,transactions:rows.results||[],summary,monthly:monthly.results||[],categories:categories.results||[]});}
export async function onRequestPost({request,env}){try{const denied=await requireAuth(request,env);if(denied)return denied;const t=parse(await readJson(request));const row=await env.DB.prepare(`INSERT INTO cash_transactions(client_id,project_id,type,category,description,amount_cents,due_date,paid_date,status,payment_method,notes) VALUES(?,?,?,?,?,?,?,?,?,?,?) RETURNING id`).bind(t.client_id,t.project_id,t.type,t.category,t.description,t.amount_cents,t.due_date,t.paid_date,t.status,t.payment_method,t.notes).first();await env.DB.prepare(`INSERT INTO crm_activities(entity_type,entity_id,action,details) VALUES('receipt',?,'cash_created',?)`).bind(row.id,t.description).run();return json({id:row.id},201);}catch(e){return errorResponse(e);}}
export async function onRequestPut({request,env}){try{const denied=await requireAuth(request,env);if(denied)return denied;const body=await readJson(request),id=integer(body.id,{min:1}),t=parse(body);const result=await env.DB.prepare(`UPDATE cash_transactions SET client_id=?,project_id=?,type=?,category=?,description=?,amount_cents=?,due_date=?,paid_date=?,status=?,payment_method=?,notes=?,updated_at=datetime('now') WHERE id=?`).bind(t.client_id,t.project_id,t.type,t.category,t.description,t.amount_cents,t.due_date,t.paid_date,t.status,t.payment_method,t.notes,id).run();if(!result.meta.changes)throw new HttpError(404,'Lançamento não encontrado');return json({ok:true});}catch(e){return errorResponse(e);}}
export async function onRequestDelete({request,env}){try{const denied=await requireAuth(request,env);if(denied)return denied;const id=integer(new URL(request.url).searchParams.get('id'),{min:1});await env.DB.prepare(`UPDATE cash_transactions SET status='cancelled',updated_at=datetime('now') WHERE id=?`).bind(id).run();return json({ok:true});}catch(e){return errorResponse(e);}}
